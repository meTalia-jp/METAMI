# M.E.T.A.M.I. GitHub公開前チェックリスト

GitHubへの初回push直前に、作業者が各項目を確認してください。
チェックできない項目がある場合は公開を止め、理由を記録します。

## 個人情報・秘密情報

- [x] 管理対象ファイルに個人名、住所、メールアドレス等が含まれていない
- [x] 公開文書・ソースコードに開発PCの絶対パスが含まれていない
- [x] APIキー、トークン、パスワード、秘密鍵が含まれていない
- [x] ユーザーのメモや生成Promptを含む実DBが追跡されていない
- [x] ログ、クラッシュダンプ、キャッシュが追跡されていない

## 画像・動画・データ

- [x] `sample/`の私物画像・動画がGit管理されていない
- [x] `data/metami.db`とSQLite補助ファイルがGit管理されていない
- [x] テスト生成物、出力ファイル、バックアップが追跡されていない
- [x] 公開画像に個人情報、ローカルパス、実ファイル名が映っていない
- [x] キャラクター素材の公開条件を文書化した
- [x] GUIモックアップ4画像を公開対象外にした
- [x] キャラクター画像・モックアップ・公開画像に第三者素材がないことを確認した

## 環境・依存関係

- [x] N100／16GB別PCのPython 3.12環境で確認した
- [x] `python -m venv .venv`が成功した
- [x] `python -m pip install -r requirements.txt`が成功した
- [x] `requirements.txt`が実行時依存だけを含む
- [x] `python src/main.py`で起動した
- [x] `run_metami.bat`で起動した
- [x] 新規DBが`data/metami.db`へ自動作成された
- [x] 既存Version 3 DBを壊さず起動できた

## 機能・安全性

- [x] PNG、WEBP、JPG/JPEG、MP4を読み込めた
- [x] 横長、縦長、1:1カードが正しく表示された
- [x] 画像・動画がアスペクト比を維持して表示された
- [x] 評価、タグ、メモ、ユーザータイトルが保存・復元された
- [x] missing、再関連付け、METAMI記録削除を確認した
- [x] WorkflowとJSON表示を確認した
- [x] 原本ファイルのハッシュが操作前後で一致した
- [x] キャラクター画像欠損時も起動した
- [x] 最終差分で`python -m unittest discover -s tests -v`が成功した

## 文書・ライセンス

- [x] `README.md`がVer1.0.0候補の仕様と一致する
- [x] 別PCセットアップ手順を実行できた
- [x] 既知の制約をREADMEへ記載した
- [x] ソースコード用MIT Licenseをユーザーが決定した
- [x] `LICENSE`へ`Copyright (c) 2026 meTalia-jp`を配置した
- [x] `NOTICE.md`を最終版へ更新した
- [x] `src/assets/README.md`へキャラクター利用方針を記載した
- [x] Pythonソース配布時のPySide6／Qt for Python方針を確定した
- [x] PySide6、Shiboken6、Qtバイナリをリポジトリへ同梱しない
- [x] 将来の実行ファイル・Qtライブラリ同梱配布を今回の判断対象外とした

## Git

- [x] `git status --short`が意図した状態になっている
- [x] `git diff`と`git diff --cached`を確認した
- [x] 実DB、私物データ、秘密情報がGit履歴にない
- [ ] コミットメッセージを確認した
- [ ] GitHubへのpush先と公開範囲を確認した
- [ ] push前に最終バックアップを作成した

## 公開操作直前に残る項目

- [ ] 既存Git履歴に含まれるGUIモックアップ4画像を公開しない方法を確定する
- [ ] READMEの画像3枚をGitHub上のプレビューで確認する
- [ ] 最終監査・試験結果を反映してコミットする
- [ ] GitHubへのpush先と公開範囲を確認する
